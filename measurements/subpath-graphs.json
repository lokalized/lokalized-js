{
  "note": "Per-subpath module graphs. Module count and source bytes ratchet; growth needs a reason.",
  "generatedBy": "tools/subpath-graphs.mjs",
  "history": [
    {
      "reason": "Initial baseline, recorded after M8 S2/S3 landed the directory loader. lokalized/load and lokalized/ssr are still export-{} scaffolds at 1 module each; lokalized/node is 27 modules because the loader composes the shipped parser rather than duplicating it."
    },
    {
      "reason": "M8 S5: lokalized/load's type surface authored from the plan's own TypeScript blocks — CatalogIdentity, StringsManifestV1, FetchEntry, CatalogIdentityInputV1, StringsLoadCoverage, LoadedStrings and LoadFailure. Types only; no runtime, so the graph gains the subpath's own bytes and no new module."
    },
    {
      "reason": "M8 S6: computeCatalogIdentity, plus the synchronous SHA-256 and the bounded RFC 8785 canonicalizer plan 6.1 gives the load graph so identity stays synchronous in a browser. lokalized/load 1 -> 4 modules; the ROOT graph is unchanged at 25 modules, which is the containment claim this ratchet exists to check."
    },
    {
      "reason": "M8 S6b: parseStringsManifest, validateStringsManifest and localeConfigurationForManifest. The load graph gains the bounded JSON path, the CLDR tag data and the locale machinery it validates manifest tags against; the ROOT graph is unchanged."
    },
    {
      "reason": "M8 S7: chain and fetchSet. One module, and the graph barely moves because the candidate sequence is REUSED from src/internal/locale.js rather than reimplemented for the manifest door."
    },
    {
      "reason": "M8 S8: loadStrings and loadEntireManifest. The load graph gains src/parse and its catalog machinery, because a fetched body is parsed by the SAME bounded parser every other door uses rather than a second one."
    },
    {
      "reason": "M8 S9a: the runtime CLDR/data compatibility check plan :1893 requires before catalog I/O. lokalized/load gains src/data/provenance.js — ONE module — which slice S6b had wrongly declared unreachable."
    },
    {
      "reason": "M8 S9: the loaded branch reaches core and therefore every published subpath that re-exports it."
    },
    {
      "reason": "M8 S10: the SSR stamp ships (lokalized/ssr 1 -> 2 modules), and core gains src/internal/runtime-metadata.js — the producer/IANA/corpus identity a stamp must take from the RENDERER, which only works if the renderer holds it. Root/core +1 module; the load subpath grows only by fetch-loader emitting manifestLocaleConfiguration."
    },
    {
      "reason": "M8 S11a: lokalized/node gains the manifest-mediated file loaders, so it now reaches the whole lokalized/load graph (manifest validation, planning, identity, SHA-256, JCS) — 27 -> 37 modules. lokalized/load gains src/load/run-plan.js, the plan runner both doors share: plan order, the concurrency cap, the partial-failure policy, abort and digest-before-parse live in ONE place and the door supplies only a transport."
    },
    {
      "reason": "M8 S11b: the directory walk EXTRACTED to src/node/discovery.js so both Node doors drive one enumeration, plus createStringsManifestFromDirectory. lokalized/node 37 -> 39 modules — discovery.js and manifest-directory.js — and no other subpath moves, because the generator reuses the load graph's identity, SHA-256 and manifest validation rather than duplicating them. (Reason text reconstructed 2026-09-11 after a review session reverted this file by accident; the measured numbers are re-derived from the same source.)"
    },
    {
      "reason": "M8 S11b review fixes: normalizedTiebreakers plus the per-file manifest-tag check in the generator, and manifest.js refusing a Map where every read is Object.entries."
    },
    {
      "reason": "M8 S9-owed: core gains the applicable-configuration split (the selection channel reads the full manifest, the resolution channel the loaded catalogs) and the per-use lookup-coverage check. No new module — source only."
    },
    {
      "reason": "M8 close-out: the nine promised-but-undeclared type names land in core and negotiate, and tools/gen-data.js now emits all five fields plan 2.2 declares for DataProvenance (the shipped provenance carried two, so the type could not be declared truthfully)."
    },
    {
      "reason": "M8 clause 30: manifest tiebreakers validated against the full manifest (src/load/manifest.js) and filtered to loaded tags in the shared runner (src/load/run-plan.js). No new modules; both graphs grew by the same 5,986 bytes because lokalized/node composes lokalized/load."
    },
    {
      "reason": "M8 clause 22: the plan runner now checks options.signal at the top of its worker loop, so aborting stops ADMISSION rather than relying on the transport to refuse. 748 bytes, no new modules; node composes load."
    },
    {
      "reason": "M8 clause 19: the loaded branch now refuses a record whose two copies of its own warning list disagree (src/internal/loaded-input.js). 3,725 bytes, no new modules; it is in the root graph, so every subpath that composes core moves."
    },
    {
      "reason": "M8 clauses 10 and 33: the Fetch door gained the scheme preflight its Node sibling already had (src/load/fetch-loader.js), and the loaded branch now hands construction the option name it actually reads (src/internal/loaded-input.js)."
    },
    {
      "reason": "M8 clause 75: StringsLoadingError gains the construction token StringsParseError has had since M5, and DigestUnavailableError becomes a real Error subclass instead of a plain Error with its name assigned afterwards. No new modules."
    },
    {
      "reason": "S28: src/load/index.js stops DECLARING core's CatalogIdentity and StringsLoadCoverage and consumes them inline, so lokalized/load no longer re-exports the two types plan 3.1 says it must not re-own. The two typedef blocks became prose recording why; +439 B on load, +6 B on node. No module added to any graph."
    },
    {
      "reason": "S29: plan 3.5:1092-1100 declares nine error classes as package exports and the port shipped three. DigestUnavailableError now exports from lokalized/load and ExpressionEvaluationError from lokalized/core; the latter gained the construction token plan 3.5:1107 requires, plus an expressionEvaluationError() factory because it is raised from three modules. No module added to any graph; the growth is the guard, the factory and the prose recording why."
    },
    {
      "reason": "S29: createStrings gains the OPTION UNION plan 3.2:565-588 declares. The port had only the direct arm and read the loaded member through an any cast, so createStrings({ loaded, locale }) -- M8's flagship call -- failed to typecheck for a TypeScript consumer (TS2353). Both arms now spell the other's members never, so a mixed call is refused too. Growth is the two typedefs and the prose recording the defect."
    },
    {
      "reason": "S30: plan 3.2:447-453's seven build-identity constants now export from lokalized/core (cldrVersion, dataFingerprint, behavioralVectorsVersion, localeDataMode, cardinalityMode, ianaRegistryDate, ianaDataFingerprint). The values already existed in RUNTIME_METADATA and the pinned provenance module, both already in core's graph, so no module is added; the growth is the seven bindings and the docblock recording that two independent gates found them missing."
    },
    {
      "reason": "S34: ConfigurationError becomes a real token-guarded class and exports from lokalized/core. Plan 3.5:1100 declares it; 119 call sites across core, load, ssr and node raised a plain Error with name assigned afterwards, catchable only by string. No module added; the growth is the class, the token, the factory and the prose recording the third instance of this shape."
    },
    {
      "reason": "S35: plan 3.5:1092's LokalizedError base class lands in src/internal/lokalized-error.js, and all seven library error classes now extend it, so one instanceof answers 'did this come from lokalized'. +1 module in every graph that can fail — which is every graph, because a base class is reachable from every error that extends it. UnsupportedLocaleError also gained the token, the UNSUPPORTED_LOCALE code plan 3.5:1050 declares, and the plan's field name (locale, not localeTag)."
    },
    {
      "reason": "BATCH: plan 3.7's tagged-value type family (15 names) plus seven more promised symbols land, and the 61 language-form constants gain exact per-constant types. They were emitted as Readonly<{...}> | undefined with axis and name widened to string, because src/index.js built them by looping into a Record; a consumer had to non-null-assert a frozen compile-time constant and could not name the axis. No module joins any graph — the 61-key map lives in internal/catalog.js, which every graph already reaches. The growth is JSDoc."
    },
    {
      "reason": "Decision batch D1-D10: the manifest door now refuses zero-candidate and ambiguous fallbacks at validation (plan 6.2:145, both arms, before any per-file plan exists); every library error constructor is private or protected so the declarations expose no constructor or extension signature (plan 3.5:1107); dead ResolutionFailure class deleted. Growth is the two refusal arms, the seven static raisers and the prose recording each decision."
    },
    {
      "reason": "D8: plan 3.5's ninth and last error class lands. ResolutionError carries Java's two recognized resolution categories, ~28 leaf raisers across three files take their codes from the executed oracle, and both contextualizing ladders now have Java's arms in Java's order — including ARM 4, which returns an unrecognized APPLICATION exception unchanged, as plan 2.5:249 requires and the port did not do. +1 module (src/internal/resolution-error.js), reachable from every graph that can fail."
    },
    {
      "reason": "M9 examples: run-plan.js gained the annotation record that makes a loader result assignable to createStrings({ loaded }), plus the comment recording how it was found"
    },
    {
      "reason": "M9 S2: lokalized/negotiate gains plan 3.4:904-915's forLanguageRanges, forAcceptLanguage and the two IANA constants, which pull in the zero-import internal/runtime-metadata.js leaf (15 -> 16 modules)"
    },
    {
      "reason": "M9 S3: src/parse/index.js gains plan 3.6:1477's mergeParsedStringsFiles, the last promised-and-undelivered name, with the Java-measured equality predicate; parse/load/node all reach that module"
    },
    {
      "reason": "M9 S5: plan 2.2:150's candidate-chain LRU lands in src/internal/locale.js (the memo factory and its two test-only probe symbols) and src/core/index.js (construction and the probe); every subpath reaches the locale kernel"
    },
    {
      "reason": "M9 S6: src/load/fetch-loader.js exports DEFAULT_REQUEST so test/example-server.test.js can pair the loader's CORS mode with the preload's bare crossorigin attribute — plan 6.3:2180 makes those one claim in two places"
    },
    {
      "reason": "M9 S8: src/negotiate/index.js renders a refused weight with javaDoubleText on the object door, matching the header door and Java's Double.toString; the comment records the measurement that found it"
    },
    {
      "reason": "M9 S9: jdkLanguageSubtag maps the superseded ISO codes FORWARD to the modern spelling (the pinned JDK 21 behaviour) and both inverted comments are corrected with their measurements"
    },
    {
      "reason": "M8 clause 78: src/load/run-plan.js releases the parse slots on every rejection path (a held StringsLoadingError retained 5 of 5 parsed catalogs through the error's captured stack; measured 0 after). +1,347 bytes, of which the mechanism is three lines and the rest is the comment explaining why a memory property is being asserted in a loader."
    },
    {
      "reason": "src/load/manifest.js: an inline comment reading 'only the comparison is missing' sat seven lines above the comparison it said was missing — a stale self-asserting text in shipped source, found by an agent measuring the data-mismatch surface. Replaced with two lines stating why the shape check precedes the comparison; +88 bytes in the load and node graphs, no behaviour change."
    },
    {
      "reason": "M-D S31: re-recorded after the new SHRINK report surfaced a stale artifact. The record read 872,639 bytes for lokalized against a live 872,635 — four bytes, from a comment reworded in S30 — because this ratchet only ever failed on growth and said nothing about a shrink. Found by tools/bundle-sizes.mjs, which ran a real bundler over the same graphs and agreed with the tree while disagreeing with this file."
    },
    {
      "reason": "M-D S32: two stale graph counts removed from shipped source docblocks — src/index.js said the root 'stays 25 root / 24 core' in the present tense (it is 31/30) and src/internal/interpolate.js said scenario:0a 'ratchets at 19 modules'. Both propositions held; only the figures had not been re-read since M2, so the numbers are gone and the propositions stay."
    },
    {
      "reason": "M-D S33: four maintainer decisions land together — the manifest format carries all seven build-identity fields (runtime-metadata.js joins the load and node graphs, +1 module each), every public door refuses an unknown option (configuration-error.js joins the parse graph via catalog.js, +1 module), matchType is narrowed to plan 3.4's LocaleMatchType (JSDoc only), and the onWarning observer is documented. No behaviour moved that the corpus can see: conformance is 2,150/0/0 across all four."
    },
    {
      "reason": "M-D S34: the four aggregate load budgets are charged ACROSS a load at the manifest doors, as the types and README have always documented and as the directory door has always done. runPlan now gives each file its own LoadingSession (unchanged as a per-file bound, new as a measurement) and reconciles the four counters in FETCH-PLAN order after the workers finish, replaying through the session's own guarded methods so both door families raise one sentence. lokalized/load 39 -> 38 and lokalized/node 42 -> 41 modules: the runner calls the INTERNAL parseStringsWithSession, so the public parse door leaves both loader graphs. Root/core/parse grow by the shared LoadingSession.absorb. conformance byte-identical at 2,150/0/0."
    },
    {
      "reason": "M-R S3: TranslationFailure/FallbackEvent localeMatch typed Readonly<LocaleMatch> and StringsLoadingError.failures typed LoadFailure; documentation comments only, no runtime change"
    },
    {
      "reason": "M-R: every returned LocaleMatch and language-range list is deep-frozen at its one construction site in matchForRanges, and parseLanguageRanges freezes its members; LocaleMatch and WeightedLanguageRange are each a single Readonly<...> typedef that core and negotiate derive instead of authoring their own mutable copies."
    },
    {
      "reason": "M-R S3: the consumer-facing contract defects the requirement registry exposed. Returned values are now frozen one level down — getResult().localeMatch's consideredLocales, languageRange and requestedLanguageRanges (and its members) were mutable, so a consumer could write into a record the library handed them; the typedef's arrays became readonly to match what the runtime now enforces. StringsLoadingError.failures is typed LoadFailure[] rather than any[], and the runner's internal cast narrowed from stage?: string to the seven-member union. TranslationFailure.localeMatch and FallbackEvent.localeMatch are typed Readonly<LocaleMatch> rather than unknown — both are handed to consumer callbacks. All JSDoc and freeze calls; conformance byte-identical at 2,150/0/0."
    },
    {
      "reason": "M-R S4: the registry's 179 readonly statements land. Fourteen JSDoc typedefs wrapped in Readonly<> with their per-property documentation restated as prose in the docblock (which is what grew the source), nine error-class fields marked @readonly, the code threaded through LokalizedError's constructor, and five parameter element types narrowed from unknown."
    },
    {
      "reason": "M-R S5: the Unicode attribution becomes a preserved legal comment on each of the fifteen generated CLDR modules, so it survives minification into whatever a consumer bundles. ~2.1 KB of source per graph; esbuild deduplicates the identical banners to one copy in the artifact."
    },
    {
      "reason": "M-R S8: likely-subtags.js is re-encoded structurally — 7,788 rows in 25 KB of source instead of 158 KB, decoding to output byte-identical to the pinned CLDR artifact. The browser root bundle falls 322,509 -> 187,679 minified and 73,653 -> 55,610 brotli, -24.5% on the wire. Conformance byte-identical at 2,150/0/0."
    }
  ],
  "subpaths": {
    "lokalized": {
      "modules": 31,
      "bytes": 764556
    },
    "lokalized/core": {
      "modules": 30,
      "bytes": 753943
    },
    "lokalized/parse": {
      "modules": 29,
      "bytes": 573868
    },
    "lokalized/load": {
      "modules": 38,
      "bytes": 658048
    },
    "lokalized/ssr": {
      "modules": 3,
      "bytes": 33699
    },
    "lokalized/negotiate": {
      "modules": 16,
      "bytes": 288060
    },
    "lokalized/node": {
      "modules": 41,
      "bytes": 692468
    },
    "lokalized/data/ordinal": {
      "modules": 33,
      "bytes": 784282
    },
    "lokalized/data/ranges": {
      "modules": 33,
      "bytes": 789155
    }
  }
}
